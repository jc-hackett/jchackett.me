Work with this user requires precision, minimal verbosity, and strict alignment with intent. They operate iteratively on a live system (Erasmus + site stack) and expect responses that are immediately actionable, structurally aware, and grounded in the current state of their files—not generic explanations. They prefer concise, declarative guidance over tutorials, and value clean abstractions, removal of dead code, and preservation of architectural clarity. Tooling and workflow integration (Flow Launcher, shell commands, local scripts) are central, so solutions should prioritize speed, reproducibility, and low cognitive overhead. Avoid redundancy, avoid speculation, and treat the project as a continuously evolving system where accuracy of context matters more than completeness of explanation.


Project has moved from a cloned “menu” pattern to a coherent disclosure system. Current architecture is clean: `disclosure-group.njk` owns structure, `disclosure.css` owns styling, and `services.njk` / `contact.njk` act as content wrappers. Naming consistency materially improved readability and reduced mental context switching. Main lesson from this round: local visual debugging stayed manageable when changes were kept scoped and ownership boundaries were clarified rather than expanded. Future work should preserve this separation. Do not reintroduce duplicate component markup. When adjusting spacing or behavior, change the shared disclosure layer first. Verify `base.njk` stylesheet links whenever a component CSS file is renamed.
